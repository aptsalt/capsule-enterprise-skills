# capsule status — my-project (ven) vs enterprise master

Generated by `capsule status` (compares `my-project/capsule.lock` against the
enterprise `master` registry). Skills are distilled from real Claude Code
sessions via local Ollama; the durable memory lives in Backboard.

## 1 update available from enterprise

| Skill | Pinned (local) | Latest (enterprise master) | Bump | Learned from |
|-------|----------------|----------------------------|------|--------------|
| `command-verification` | **1.0.0** | **2.0.0** | major | CAP-R005 — verify current state via official docs, not inference (session `52cdf3c2`, project Beacon) |

Up to date:

| Skill | Pinned (local) | Enterprise master | Learned from |
|-------|----------------|-------------------|--------------|
| `api-security`      | 1.0.0 | 1.0.0 | CAP-R006 — always use valid auth credentials to avoid repeated 401s |
| `memory-management` | 1.0.0 | 1.0.0 | CAP-R008 — allocate enough memory for large model loads to avoid silent OOM |

## Contribution status (this branch)

| Capsule | Target skill | Gate | Pipeline outcome |
|---------|--------------|------|------------------|
| `CAP-V001` | `skill/command-verification` | novelty 31 / transfer 44 | **DEDUP** — semantic signature matches CAP-R007; merged into canonical 1.0.0, ven + dee co-credited (no new version) |

CAP-V001 did **not** clear the novelty/transfer gate as a *new* finding — and it
didn't need to. It was a **re-discovery** of an existing canonical finding, so
the pipeline deduped it and recorded ven on the provenance instead of forging a
redundant version. See `contributed-capsules/RESOLUTION.md`.

## To adopt the master head

```bash
capsule pull command-verification@2.0.0   # then update capsule.lock
```
