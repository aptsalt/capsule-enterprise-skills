# capsule status — my-project vs enterprise master  (ven)

Generated by `capsule status` (compares `my-project/capsule.lock` against the
enterprise `master` registry). Skills are distilled from real Claude Code sessions
via local Ollama; the durable memory lives in Backboard. `master` now indexes
**27 skills** (12 capsule-distilled + 15 newly seeded top-15 popular skills).

## Newly pinned this update — ven's infra / devops role pack (5 skills)

All pinned at the freshly published `1.0.0` and **current with enterprise master**:

| Skill | Pinned (local) | Enterprise master | Seeded from |
|-------|----------------|-------------------|-------------|
| `docker-containerization` | **1.0.0** | 1.0.0 | seeded from community best practice (top-15 popular infra / devops) |
| `kubernetes-orchestration` | **1.0.0** | 1.0.0 | seeded from community best practice (top-15 popular infra / devops) |
| `ci-cd-pipelines` | **1.0.0** | 1.0.0 | seeded from community best practice (top-15 popular infra / devops) |
| `structured-logging` | **1.0.0** | 1.0.0 | seeded from community best practice (top-15 popular infra / devops) |
| `observability-tracing` | **1.0.0** | 1.0.0 | seeded from community best practice (top-15 popular infra / devops) |

These five are ven's unique slice of the top-15 popular catalog; the other ten
popular skills are pinned by the sibling dev repos.

## Pre-existing pins (unchanged)

See the `[skills]` block above this update in `capsule.lock` for ven's prior pins
and any intentionally-older versions / contributed capsules — those are untouched by
this role-pack update.

## To adopt the role pack

```bash
capsule pull docker-containerization@1.0.0
capsule pull kubernetes-orchestration@1.0.0
capsule pull ci-cd-pipelines@1.0.0
capsule pull structured-logging@1.0.0
capsule pull observability-tracing@1.0.0
# pins recorded in my-project/capsule.lock
```

This is the enterprise-vs-personal compounding story: the registry on `master` grew
by 15 popular skills, and ven pinned the 5 aligned to the **infra / devops** track — pinned,
reproducible, and updated only when ven chooses.
