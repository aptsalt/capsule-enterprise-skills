# dee — usage

This personal repo pins specific enterprise skill versions for the backend / API workstream.

- Pull a skill:  `capsule pull skill/<id>@<version>`
- Update to latest enterprise:  `capsule update`
- Propose a local upgrade back to enterprise:  open a PR `dee -> master` (agentic CI gates the merge).
