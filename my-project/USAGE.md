# saim — usage

This personal repo pins specific enterprise skill versions for the data / quality workstream.

- Pull a skill:  `capsule pull skill/<id>@<version>`
- Update to latest enterprise:  `capsule update`
- Propose a local upgrade back to enterprise:  open a PR `saim -> master` (agentic CI gates the merge).
